# ============================================================
# Core data stack
# ============================================================
pandas>=2.1,<3.0
numpy>=1.26,<2.0
scipy>=1.11,<2.0
pyarrow>=14.0          # parquet I/O

# ============================================================
# Machine learning
# ============================================================
scikit-learn>=1.4,<2.0
lightgbm>=4.1,<5.0
xgboost>=2.0,<3.0
mord>=0.7              # ordinal regression (LogisticAT, LogisticIT)
imbalanced-learn>=0.12 # SMOTE, SMOTE-NC, etc.
joblib>=1.3            # model persistence

# ============================================================
# Interpretability & fairness
# ============================================================
shap>=0.44
fairlearn>=0.10        # disaggregated metrics by demographic group

# ============================================================
# Visualization
# ============================================================
matplotlib>=3.8
seaborn>=0.13
plotly>=5.18           # interactive plots in app

# ============================================================
# UI
# ============================================================
streamlit>=1.30

# ============================================================
# Notebooks & exploration
# ============================================================
jupyter>=1.0
ipykernel>=6.28
ipywidgets>=8.1

# ============================================================
# Environment & utilities
# ============================================================
python-dotenv>=1.0
tqdm>=4.66
pyyaml>=6.0
click>=8.1             # CLI for src scripts

# ============================================================
# Development tools
# ============================================================
pytest>=8.0
ruff>=0.4              # fast linter + formatter
black>=24.0            # opinionated formatter (optional with ruff)

# ============================================================
# Optional — uncomment as needed
# ============================================================
# anthropic>=0.30                # LLM verbatim generation (Phase 5)
# openai>=1.30                   # alternative LLM provider
# sentence-transformers>=2.7     # text embeddings (Phase 8)
# mlflow>=2.10                   # experiment tracking
# tabpfn>=2.0                    # tabular foundation model (bonus)
# evidently>=0.4                 # drift monitoring (Phase 13)
# kaggle>=1.6                    # CLI to download dataset
